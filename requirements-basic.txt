# Minimal dependencies to run training and dataset code

torch>=2.0.0
transformers>=4.36.0
peft>=0.7.0
accelerate>=0.24.0
datasets>=2.14.0
jsonlines>=4.0.0
tqdm>=4.65.0
python-dotenv>=1.0.0

# Optional but commonly needed for HF models/tokenizers
safetensors>=0.4.0
